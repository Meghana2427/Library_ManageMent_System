# Library_ManageMent_System
Menu-driven Library Management System in C using structures, dynamic memory allocation, linked lists, file handling, and Makefile. It supports adding, listing, searching, sorting, counting, and borrowing books with persistent storage across program executions using modular functions.
